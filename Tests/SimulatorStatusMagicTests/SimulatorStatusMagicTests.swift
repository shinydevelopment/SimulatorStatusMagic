import XCTest
@testable import SimulatorStatusMagic

final class SimulatorStatusMagicTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimulatorStatusMagic().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
